{Nota: enunciado original no identificado; descripción inferida del código. Programa trivial
(writeln('Cosa') con dos bloques de código comentados), sin relación temática con el ejemplo del centro de
deportes que lo acompaña en la misma carpeta; probablemente un archivo de prueba/scratch. No compila en
este entorno por un byte no-ASCII presente en el archivo original (Fatal: illegal character); se preserva
tal cual.}

program ejercicio;
var
   {y : integer;
   z : real;}
begin
    writeln('Cosa');
   {read(z);
   y := z;
   z := z * 10;}
end. 